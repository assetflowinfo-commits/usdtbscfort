{
  "name": "bsc-fork-tutorial",
  "version": "1.0.0",
  "description": "Fork BSC Mainnet to test USDT",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js",
    "fork": "npx hardhat node --fork https://bsc.publicnode.com",
    "setup": "npx hardhat run setup-usdt.js --network localhost"
  },
  "dependencies": {
    "hardhat": "^2.19.0",
    "dotenv": "^16.0.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "ethers": "^5.7.0"
  }
}